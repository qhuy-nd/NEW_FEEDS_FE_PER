const PREFIX_DEPARTMENT = "/department";

export const API_DEPARTMENT_ROUTERS = {
  GET: {
    GET: `${PREFIX_DEPARTMENT}/action/get/{slug}`,
    GETLIST: `${PREFIX_DEPARTMENT}/action/getlist`,
  },
};
